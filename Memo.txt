20181102
UTF-8でファイルを作成。ブランチのテストのためにいろいろと遊んでみることにする。
SourceTreeから「現在のバージョンを開く」（ファイルのダブルクリック？）では、RadRxでは、秀丸でファイルが開かれた。.txtの関連付けが秀丸になっているせいか。
BOMを入れていないせいか、shif-JISをデフォルトに設定したvsCodeで開くと、shift-jisとして開かれてしまう。BOMを付けてあればいいのだと思うが。
vsCodeのデフォルトをUTF-8に戻すかどうかは、検討が必要。

20181106
MacのRadRxでは、vsCodeのデフォルトをUTF8にもどした。

originalへの追加
Macブランチをつくって、追加している。
TestBranchを作成し、そこで修正を加えている。
さらに修正を加えている
TestBranchをチェックアウトして修正を追加

ブランチ2で追加

ローカルで作業を行うときは、ブランチを作成して作業を行うのがいい。コミットしてプッシュしてもoriginalはそのまま残る。
いろいろと作業を行ってローカルコミットを行い、一日の終わりなどにバックアップを兼ねて、プッシュしておくのがいいだろう。
